language: perl
perl:
  - "5.26"
before_install:
    - eval $(curl https://travis-perl.github.io/init) --auto
    - local-lib
install:
    - carton install
script:
    - carton exec -- prove -lv $(test-dirs)
